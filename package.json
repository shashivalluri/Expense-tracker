{
  "name": "glassmorphic-expense-tracker-workspace",
  "version": "1.0.0",
  "description": "Full-stack Budget Tracker Pro workspace",
  "main": "api/index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:client\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd client && npm run dev",
    "seed": "cd backend && npm run seed",
    "build": "cd backend && npm install --include=dev && cd ../client && npm install --include=dev && npm run build",
    "vercel-build": "cd backend && npm install --include=dev && cd ../client && npm install --include=dev && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {}
}
